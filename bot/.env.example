# Copy to ".env" and fill in. Then: source .env && python review_bot.py
# (or: set these in your host's environment / dashboard)

export DISCORD_TOKEN="paste-your-new-bot-token"
export GITHUB_TOKEN="paste-a-token-with-issues-readwrite"
export CHANNEL_ID="paste-the-channel-id"

# Optional overrides:
# export GITHUB_REPO="cursorvoice/community"
# export BOT_NAME="Cursor Voice Submissions"
# export LOGO_URL="https://cursorvoice.app/favicon-180.png"
# export APPROVER_IDS="123456789012345678"   # only these Discord user IDs can approve
