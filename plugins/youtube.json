{
  "name": "search youtube",
  "description": "Search YouTube for videos matching a query.",
  "parameters": { "type": "object", "properties": { "query": { "type": "string" } }, "required": ["query"] },
  "run": { "type": "open_url", "template": "https://www.youtube.com/results?search_query={{query}}" }
}
